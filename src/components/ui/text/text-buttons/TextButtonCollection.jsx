import classes from "./TextButtonCollection.module.scss";

const TextButtonCollection = ({ className }) => {
  return <span className={classes.btn}>Collection</span>;
};

export default TextButtonCollection;
